# KLARA OS — run: pip install -r requirements.txt
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
PuLP>=2.7.0

# Optional: Gurobi solver (install separately if you have a license)
# gurobipy
